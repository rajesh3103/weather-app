services:
  - type: web
    name: weather-app
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: WEATHER_API_KEY
        value: b9dd7e4750f54e99
        
